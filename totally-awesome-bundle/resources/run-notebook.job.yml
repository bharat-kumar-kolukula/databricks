resources:
  jobs:
    run_notebook:
      name: demo
      queue:
        enabled: true
      tasks:
        - task_key: helloworld
          notebook_task:
            notebook_path: totally-awesome-bundle/helloworld.py
      email_notifications:
        no_alert_for_skipped_runs: true
        on_failure:
          - bharatkumarkolukula@gmail.com
      #trigger:
        # Run this job every day, exactly one day from the last run; see https://docs.databricks.com/api/workspace/jobs/create#trigger
        #periodic:
          #interval: 1
          #unit: DAYS


