# Databricks Asset Bundle configuration, more info here: https://docs.databricks.com/en/dev-tools/bundles/settings.html
bundle:
  name: "totally-awesome-bundle"

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  # The 'dev' target, for development purposes. This target is the default.
  dev:
    mode: development
    default: true
    workspace:
      host: https://dbc-99510b43-f14c.cloud.databricks.com
    run_as:
      user_name: bharatkumarkolukula@gmail.com

  # The 'prod' target, used for production deployment.
  prod:
    mode: production
    workspace:
      host: https://dbc-99510b43-f14c.cloud.databricks.com
      # We always use /Users/bharatkumarkolukula@gmail.com for all resources to make sure we only have a single copy.
    run_as:
      # This runs as bharatkumarkolukula@gmail.com in production. We could also use a service principal here,
      # see https://docs.databricks.com/dev-tools/bundles/permissions.html.
      user_name: bharatkumarkolukula@gmail.com
